<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Needed for a direct home-LAN Home Assistant URL. Prefer HTTPS when available. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
